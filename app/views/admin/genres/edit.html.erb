<h1>ジャンル編集</h1>
<%= form_with model:@genre, url: admin_genre_path(@genre.id), local:true do |f| %>
  <div class_field>
    <h3>ジャンル名</h3>
    <%= f.text_field :name, class:"form-contr_l name" %>
  </div>
  <%= f.submit '変更を保存する', class:"btn btn-success" %>
<% end %>