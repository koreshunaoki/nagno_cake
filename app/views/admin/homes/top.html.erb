<h1>注文履歴一覧</h1>

<table class="table table-hover table-inverse">
  <thead>
    <tr>
      <th>購入日時</th>
      <th>購入者</th>
      <th>注文個数</th>
      <th>注文ステータス</th>
    </tr>
    <% @orders.each do |order| %>
    <tr>
      <th><%= link_to admin_order_path(order) do %><%= order.created_at %><% end %></th>
    </tr>
    <% end %>
  </thead>

</table>

